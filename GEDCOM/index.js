/**
 * GEDCOM Module - Index file
 * Export GEDCOM reading and parsing classes
 * 
 * @author Steve Turley
 * @version 1.0.0
 */

const GedReader = require('./GedReader');
const GedModel = require('./GedModel');

module.exports = {
    GedReader,
    GedModel
};
